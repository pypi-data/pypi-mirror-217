[tool.poetry]
name = "pylatent"
version = "0.1.0"
description = "Latent related utils and scripts."
authors = ["Mert Cobanov <mertcobanov@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
