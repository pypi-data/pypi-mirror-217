[build-system]
requires = [
    "setuptools >= 65.5.1",
#    "setuptools_scm[toml] >= 6.0",
#    "setuptools_scm_git_archive",
    "wheel >= 0.38.1"
]
build-backend = 'setuptools.build_meta'

#[tool.setuptools_scm]
#write_to = "scottbrian_utils/version.py"
#git_describe_command = "git_describe --dirty --tags --long --match v* --first-parent"