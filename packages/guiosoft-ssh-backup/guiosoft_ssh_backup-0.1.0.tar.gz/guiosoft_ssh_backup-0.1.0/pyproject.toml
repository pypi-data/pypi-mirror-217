[tool.poetry]
name = "guiosoft-ssh-backup"
version = "0.1.0"
description = "SSH backup routines (client and server)"
authors = ["Guionardo Furlan <guionardo@gmail.com>"]
license = "MIT"
readme = "README.md"
packages = [{include = "ssh_backup"}]

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.group.dev.dependencies]
pre-commit = "^3.3.3"
autopep8 = "^2.0.2"
flake8 = "^6.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
