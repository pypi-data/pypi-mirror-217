from setuptools import setup

setup(name='distributions_ex_num0',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['distributions'],
      author = "HamzaTh573",
      zip_safe=False)
