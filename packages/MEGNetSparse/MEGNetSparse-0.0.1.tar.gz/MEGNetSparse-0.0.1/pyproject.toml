[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "MEGNetSparse"
version = "0.0.1"
authors = [
  { name="Romanov Ignat", email="romanov.ignat.p@gmail.com" },
]
readme = "README.md"
requires-python = ">=3.7"
dependencies=[
    "tqdm",
    "numpy",
]

[project.urls]
"Homepage" = "https://github.com/RomanovIgnat/MEGNet_pytorch_test"
