from setuptools import setup,find_packages

setup(
    name='viviLibrary',
    version='1.1.1',
    author='test user',
    description='A library for basic math operations',
    packages=find_packages()
    #install_requires
)